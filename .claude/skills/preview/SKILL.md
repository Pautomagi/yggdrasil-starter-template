---
name: preview
description: Start the dev server so you can preview your design
---

Start the development server so the designer can see their work in the browser.

1. Run `npm run dev`.
2. Tell the designer the local URL (usually http://localhost:5173).
3. Remind them they can make changes and the browser will update automatically.
